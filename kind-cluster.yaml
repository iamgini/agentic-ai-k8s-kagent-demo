kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: kagent-demo
nodes:
  - role: control-plane
    # Extra port mappings so we can reach the kagent dashboard from the host
    extraPortMappings:
      - containerPort: 8082
        hostPort: 8082
        protocol: TCP
